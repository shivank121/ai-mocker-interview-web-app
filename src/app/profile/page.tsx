import ProfileContainer from "@/frontend/containers/ProfileContainer";
import React from "react";

const page = () => {
  return (
    <div>
      <ProfileContainer />
    </div>
  );
};

export default page;
